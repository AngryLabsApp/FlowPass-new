export interface Group {
    id: string;            // uuid
    created_at: string;    // timestamp with time zone (ISO string)
    gym_id: string;        // uuid
    title: string;         // text
    updated_at: string;
    color: string;
    total_members: number;
}


